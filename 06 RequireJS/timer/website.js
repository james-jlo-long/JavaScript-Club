define([
    "lib/util",
    "lib/dom",
    "lib/events"
], function (
    util,
    dom,
    events
) {

    "use strict";

    return {
        util: util,
        dom: dom,
        events: events
    };

});
